/*
 * NavData Messages
 *
 * This contains all the text for the NavData component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NavData';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NavData component!',
  },
});
