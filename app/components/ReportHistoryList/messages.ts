/*
 * ReportHistoryList Messages
 *
 * This contains all the text for the ReportHistoryList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReportHistoryList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReportHistoryList component!',
  },
});
