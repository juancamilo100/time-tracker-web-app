import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ReportHistoryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReportHistoryPage container!',
  },
});
